import StubView from './StubView'
export default function Validation() {
  return (
    <StubView
      title="VALIDATION"
      milestone="M6"
      blurb="Baseline-relative skill from /validate — RMSE/MAE/corr and POD/FAR/CSI per model and horizon, winner highlighted, plus the per-cell Tmax error map."
    />
  )
}
