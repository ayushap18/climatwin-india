import StubView from './StubView'
export default function WhatIf() {
  return (
    <StubView
      title="WHAT-IF"
      milestone="M4"
      blurb="Perturb ΔTemp, rainfall × factor, and an urban-heat polygon, then re-simulate. Live diff heatmap and baseline-vs-scenario impact deltas via POST /whatif."
    />
  )
}
