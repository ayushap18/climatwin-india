import StubView from './StubView'
export default function Explore() {
  return (
    <StubView
      title="EXPLORE"
      milestone="M2"
      blurb="Dark vector India map with the 9×13 Delhi-NCR grid rendered as colored cells from /state — layer switch (rainfall · tmax · tmin), colorbar, and click-to-select."
    />
  )
}
